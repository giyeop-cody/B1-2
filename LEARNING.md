# B1-2 학습 노트: 컴퓨터가 갑자기 느려지거나 멈췄을 때 원인 찾아 고치기

> **문과 중졸도 이해할 수 있게** — 일상어로 풀어 설명한 학습 노트

---

## 📖 목차
1. 초심자를 위한 용어집
2. 과제 해석 및 분석
3. 과제를 진행하기 위한 기초
4. 각 기초를 익히기 위한 간단한 체험 예제
5. 과제를 작게 쪼개기: 잡 → 워크 → 워크플로우
6. 워크플로우별 트레이드오프, 이슈, 트러블슈팅
7. 과제 완료 후 학습한 내용 정리

---

## 1. 초심자를 위한 용어집

### 💻 시스템 장애 관련

| 용어 | 쉬운 설명 | 비유 |
|------|-----------|------|
| **OOM (Out of Memory)** | 메모리가 부족해서 프로그램이 강제 종료 | 책상이 꽉 차서 더 이상 작업 불가 |
| **CPU Spike** | CPU 사용률이 급증해서 시스템이 느려짐 | 한 사람이 모든 전화를 받아서 병목 |
| **Deadlock** | 두 스레드가 서로의 자원을 기다리며 멈춤 | 두 사람이 서로의 펜을 주려 안 해서 둘 다 못 씀 |
| **Watchdog** | 프로세스가 일정 시간 응답 없으면 강제 종료 | 심박수 안 보이면 제세동 |
| **RSS** | 프로세스가 실제 사용하는 물리 메모리 | 실제로 차지하고 있는 책상 면적 |
| **모니터링 로그** | 시스템 상태를 주기적으로 기록 | 환자의 혈압/맥박 기록부 |

### 📊 관제/분석 관련

| 용어 | 쉬운 설명 | 비유 |
|------|-----------|------|
| **ps** | 실행 중인 프로세스 목록 | 건물 안 사람 목록 |
| **top** | 실시간 자원 사용 현황 | 실시간 전력 패널 |
| **pstack/jstack** | 스레드별 스택 트레이스 | 각 사람이 지금 뭘 하고 있는지 |
| **GitHub Issue** | 장애 리포트를 팀과 공유하는 도구 | 장애 보고서 제출 |

---

## 2. 과제 해석 및 분석

### 2.1 한 줄 요약
**OOM, CPU Spike, Deadlock 세 가지 장애를 재현하고, 관제 데이터로 원인을 분석하여 GitHub Issue 리포트를 작성하라.**

### 2.2 핵심 도전: "데이터 기반 원인 추론"
"감"이 아닌 로그/수치로 원인을 증명. Before/After 수치로 해결을 검증.

---

## 3. 과제를 진행하기 위한 기초

### 3.1 기초 1: 메모리 관리 (OOM)
- RSS 선형 증가 = 메모리 누수
- 커널 OOM Killer가 프로세스 강제 종료
- MEMORY_LIMIT 환경변수로 한계 조정

### 3.2 기초 2: CPU 관리 (CPU Spike)
- CPU 사용률 급상승 = 무한 루프 또는 연산 과부하
- Watchdog 타이머가 SIGTERM 발생
- CPU_MAX_OCCUPY 환경변수로 임계값 조정

### 3.3 기초 3: 동시성 (Deadlock)
- Deadlock 4조건: 상호배제, 점유대기, 비선점, 순환대기
- PID는 살아있으나 응답 없음 (블록 상태)
- MULTI_THREAD_ENABLE=false로 회피

### 3.4 기초 4: 관제 데이터 분석
- monitor.sh 로그에서 수치 추출
- 시간별 변화 추이 파악
- Before/After 수치 비교

### 3.5 기초 5: GitHub Issue 리포트
- 현상 → 증거 → 원인 → 조치 → 결과 구조

---

## 4. 각 기초를 익히기 위한 간단한 체험 예제

### 4.1~4.3 전체 재현

격리된 Linux에서 다음 한 명령으로 OOM·CPU·Deadlock Before/After와 실제 gdb 스택을 순서대로 수집한다.

```bash
scripts/run-final-validation.sh
```

실험에서는 한 번에 변수 하나만 바꾼다.

- OOM: CPU=50, Thread=false 고정, Memory 256→512
- CPU: Memory=512, Thread=false 고정, CPU 100→50
- Deadlock: Memory=512, CPU=50 고정, Thread true→false

성공 문구:

```text
B1-2 FINAL RUNTIME VALIDATION: ALL PASS
```

---

## 5. 과제를 작게 쪼개기

```
Job 1: OOM 장애 재현 + 분석 + 리포트
Job 2: CPU Spike 장애 재현 + 분석 + 리포트
Job 3: Deadlock 장애 재현 + 분석 + 리포트
Job 4: 보너스 (스케줄링 알고리즘 추론)
```

---

## 6. 워크플로우별 트레이드오프, 이슈, 트러블슈팅

### 6.1 MEMORY_LIMIT 256 vs 512
- 256: 5초 만에 크래시 → 원인 파악 쉬움 but 재현 시간 짧음
- 512: 60초+ 안정 → 로그 충분 but 메모리 낭비
- **선택**: 512 (충분한 로그 확보)

### 6.2 Deadlock 재현 안 됨
- **원인**: 환경변수가 앱에 전달 안 됨
- **해결**: export MULTI_THREAD_ENABLE=true 후 앱 재시작

### 6.3 스택 트레이스 미제출 (사전평가 FAIL)
- **문제**: pstack/jstack 출력 미포함
- **해결**: pstack으로 스레드별 백트레이스 캡처하여 리포트에 첨부

---

## 7. 과제 완료 후 학습한 내용 정리

### 7.1 배운 것
- "데이터 기반 추론" — 로그 없으면 원인 파악 불가
- "Before/After 수치" — "바꿨더니 됐다"가 아닌 수치로 증명
- "GitHub Issue" — 현업의 협업 방식

### 7.2 핵심 인사이트
1. "로그가 곧 증거" — 장애 분석의 유일한 단서
2. "환경변수 하나로 장애를 재현하고 해결" — 설정의 영향력
3. "동시성 문제는 재현이 가장 어렵다" — Deadlock은 조건이 맞아야 발생
